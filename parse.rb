Class in
    #maybe initalize some object we can serialize into a JSON to convert to python?
#use our already writen python framework
    def intialize()
    end

   def load(filename)
        file = IO,readlines(filename, "\n")
        if file = []
            return false
        end
        #this chomps the \n at the end of the line
        file.each do |chomp|
            chomp.chomp!
        end
        file.each do |proc|
            if proc.match? 
   end
end
